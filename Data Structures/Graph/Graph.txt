A Graph is a non-linear data structure consisting of a fnite set of 
vertices(or nodes) and a set of edges that connect a pair of nodes

Traversals of Graphs:

Breadth-First Search(BFS): Visits nodes level by level 
Depth-First Search(DFS): Visists nodes recursively, exploring one branch at a time.